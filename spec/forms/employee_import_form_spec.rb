# frozen_string_literal: true

require 'rails_helper'

RSpec.describe EmployeeImportForm do
  context 'with valid params' do
    it 'returns success message' do
      # file_path = Rails.root.join('spec', 'fixtures', 'files', 'users-all-valid.xlsx')
      # form = EmployeeImportForm.new(file: file_path)
      # form.save
      # expect(form.success_message).to eq '3 employee imports path'
    end
  end
end
